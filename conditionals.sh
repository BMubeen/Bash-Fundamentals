#!/bin/bash
# What is it?
# Conditional operators are used to compare values.
# They help determine if a condition is true or false.
# Common operators:
# -eq → equal to
# -ne → not equal to
# -gt → greater than
# -lt → less than
# -ge → greater than or equal
# -le → less than or equal